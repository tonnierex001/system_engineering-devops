#!/usr/bin/env bash
# This script handles SIGTERM by printing a message instead of exiting

# Trap SIGTERM (15) and execute the echo command
trap 'echo "I am invincible!!!"' SIGTERM

while true; do
    echo "To infinity and beyond"
    sleep 2
done
